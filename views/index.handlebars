<h2>This is index.handlebars</h2>
<a href="/todos/new">Create</a>
<ul>
  {{#each todos}}
  <li>
    {{this.name}}
    <a href="/todos/{{{this.id}}}">detail</a>
    <a href="#">edit</a>
    <button type="submit">delete</button>
  </li>
  {{/each}}
</ul>